import React from 'react'
import { APP_NAME } from '@/lib/brand'

export const metadata = {
    title: `${APP_NAME} - Sleep`,
    description: "Having any bad dreams?",
}

function SleepPage() {
  return (
    <div>SleepPage</div>
  )
}

export default SleepPage