import React from 'react'
import { APP_NAME } from '@/lib/brand'

export const metadata = {
    title: `${APP_NAME} - Books`,
    description: "Time to read",
}

function BookPage() {
  return (
    <div>BookPage</div>
  )
}

export default BookPage