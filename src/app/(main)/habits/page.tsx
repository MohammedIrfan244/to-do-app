import React from 'react'
import { APP_NAME } from '@/lib/brand'

export const metadata = {
    title: `${APP_NAME} - Habits`,
    description: "Get on top of your habits",
}

function HabitsPage() {
  return (
    <div>HabitsPage</div>
  )
}

export default HabitsPage