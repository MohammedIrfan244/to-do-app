import React from 'react'
import { APP_NAME } from '@/lib/brand'

export const metadata = {
    title: `${APP_NAME} - Focus`,
    description: "Lets focus",
}

function FocusPage() {
  return (
    <div>FocusPage</div>
  )
}

export default FocusPage